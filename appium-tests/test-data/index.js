/**
 * Test Data Index
 * Central export point for all test data
 */

const { APP_CONFIG, TEST_USERS, ARTICLES } = require('./home.data');

module.exports = {
  APP_CONFIG,
  TEST_USERS,
  ARTICLES,
};
